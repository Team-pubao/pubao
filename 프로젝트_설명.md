# 푸바오 프로젝트 — 전체 설명서

> 한국 17개 시도의 **산업별 입지 결정 요인**을 통계적으로 분석하고, 그 결과를 인터랙티브 대시보드와 신규 산업단지 시뮬레이터로 시각화한 데이터 분석 프로젝트.

- **팀**: 푸바오 (이동혁 PM, 서찬, 박현민, 정현문)
- **소속**: 아주대학교 융합시스템공학과
- **최종 발표**: 2026-06-22 (4분)
- **분석 단위**: 17개 시도 (N=17 단년 횡단면 + 5년 패널 85 obs)

---

## 1. 한 문장 요약

> "바다(항만)·도로(IC)·전기(전력)·임금이라는 4가지 입지요인이 **어떤 산업을 어디에 모이게 하는가**"를
> 17개 시도 데이터로 통계 검정하고(가설 H1~H8), 그 결과를 **누구나 클릭해 탐색할 수 있는 웹 대시보드**로 만든 프로젝트.

핵심 질문은 단순합니다. **"신규 산업단지를 지을 때 어떤 입지 조건이 어떤 산업을 끌어들이는가?"**
이를 검증 가능한 8개 가설로 쪼개고, 각 가설을 회귀분석·공간통계·패널분석으로 검정했습니다.

---

## 2. 데이터 구조

### 종속변수 Y (설명하려는 대상)
- **KOSIS 전국사업체조사**: 시도 × 26개 산업그룹 × 2020~2024년 **사업체 수**
- 분석에는 주로 **비중**(해당 시도 전체 사업체 중 특정 산업 비율)을 사용 → `비중_{산업}`

### 독립변수 X (입지요인 4가지)
| 요인 | 컬럼 | 대상 가설 |
|---|---|---|
| 🚢 항만 접근성 | `무역항수`, `log_하역능력_합계`, `log_물동량_2023_톤` | H1 |
| 🛣️ 고속도로 IC 밀도 | `IC밀도_개당1000km2`, `IC수`, `면적_km2` | H2 |
| ⚡ 산업용 전력 | `log_산업용전력_2024` | H3 |
| 💰 평균임금 | `평균임금_2024` | H4 |

### 26개 산업그룹 — 6개 역할(축_역할)로 분류
| 역할 | 산업 예시 | 관련 요인 |
|---|---|---|
| `heavy_export` (중량·수출) | 석유화학, 1차금속, 자동차, 조선·기타운송장비 | 항만 H1 |
| `power_intensive` (전력집약) | 반도체·전자 | 전력 H3 |
| `logistics` (물류) | 도매, 소매, 운송·창고 | IC H2 |
| `capital_intensive` (자본집약) | 정보통신·IT, 금융·보험, 의료정밀·전기·기계, 전문·과학·기술 | 임금 H4(+) |
| `labor_intensive` (노동집약) | 섬유·의복·가죽, 목재·종이·인쇄 | 임금 H4(−) |
| `other` (기타) | 광업, 식음료, 건설 등 12개 | — |

### 데이터 파일 (`data/`)
| 파일 | 내용 |
|---|---|
| `01_산업매핑.xlsx` | KSIC 71개 중분류 → 26개 분석그룹 매핑 + 축_역할 메타 |
| `02_사업체수_패널.xlsx` | 5년 패널 (2210행 = 17×26×5, H7용) |
| `02_사업체수_2024.xlsx` | 2024 단년 wide (17×28) |
| `03_H1_항만.xlsx` ~ `06_H4_임금.xlsx` | 입지요인 원본 |
| `07_통합분석_2024.xlsx` | **메인 입력** (단년 통합, 17×68) |
| `08_회귀결과_*.xlsx` | 사전 계산된 회귀 β/p 매트릭스 (검증용) |
| `지도_시도경계.json` | GeoJSON 경계 (EPSG:4326, 17 시도) |
| `시군구_입지요인.csv` · `시군구_경계.geojson` | 229 시군구 4요인·경계 (지도 전용) |

---

## 3. 가설 체계 (8개) — 기본 4 + 심화 4

각 멤버가 **입지요인 1개 + 방법론 심화 1개**를 담당합니다.

### 기본 — 직접효과 (H1~H4)
| 가설 | 내용 | 방법 | 결론 |
|---|---|---|---|
| **H1** | 항만 접근성 ↑ → 중량·수출 산업 ↑ | 단순 OLS (4산업 × 3항만지표 = 12모델) | **부분지지** |
| **H2** | 고속도로 IC 밀도 ↑ → 물류·운송·창고 ↑ | 단순/다중 OLS + VIF | **지지** (R²=0.62) |
| **H3** | 산업용 전력 ↑ → 반도체·전자 ↑ | OLS (원값 vs log) + 26산업 민감도 | **부분지지** (R²≈0.13) |
| **H4** | 임금 ↑ → 자본집약(IT) ↑ / 노동집약(섬유) ↓ | OLS + 부호일치율 | **부분지지** (66.7%) |

### 심화 — 방법론 격상 (H5~H8)
| 가설 | 내용 | 방법 | 결론 |
|---|---|---|---|
| **H5** | 같은 산업이 인접 시도끼리 뭉친다 | Moran's I 공간 자기상관 (999 순열) + LISA | **부분지지** (유의 1/26) |
| **H6** | 산업별 β 순위 = 이론적 산업특성 순위 | Spearman ρ + 점이연 상관 | **부분지지** (3검정 중 1지지) |
| **H7** | 단년 OLS가 5년 패널+시도 FE에서도 유지 | PanelOLS (entity+time FE, 군집 SE) | **기각/제한적** (강건 0/16) |
| **H8** | 광역시 vs 도의 입지요인 반응 패턴 차이 | 그룹별 OLS + β차이 Z검정 | **부분지지** (유의 5/24) |

### 멤버 분담
| 멤버 | 입지요인 | 방법론 심화 |
|---|---|---|
| 이동혁 (PM) | H1 항만 | **H7** 패널 안정성 + 전체 통합 |
| 서찬 | H2 도로 IC | **H5** 공간 자기상관 |
| 박현민 | H3 전력 | **H6** 산업 이질성 (히트맵) |
| 정현문 | H4 임금 | **H8** 광역시 vs 도 분기 |

---

## 4. 코드 구조와 분석 파이프라인

### 디렉토리
```
푸바오 PROJECT/
├── CLAUDE.md                  # 프로젝트 규약 (분석 단위·가설·코딩 규칙)
├── README.md                  # 실행 가이드
├── app.py                     # ⭐ Streamlit 대시보드 (1356줄)
├── requirements.txt           # 대시보드 배포용 의존성
├── requirements-analysis.txt  # 분석 스크립트 전체 의존성
├── data/                      # 원본 정제본 (수정 금지)
├── src/                       # ⭐ 분석 스크립트
│   ├── utils/
│   │   ├── io.py              # 데이터 로드·NFC 정규화·시도명 통일·저장 헬퍼
│   │   ├── stats.py           # run_ols / VIF / 표준화 / 결론판정 공통 함수
│   │   └── fonts.py           # 한글 폰트 자동 설정 (Noto Sans KR 등)
│   ├── 00_validate_data.py    # 데이터 무결성 검증 → results/00_data_validation.md
│   ├── h1_port.py ~ h8_groups.py  # 가설별 분석 (8개)
│   └── final_report.py        # 전체 통합 + 발표 핵심 그림 생성
├── results/
│   ├── tables/                # h1~h8 가설별 xlsx 결과표
│   ├── figures/               # 발표용 PNG (산점도·히트맵·막대)
│   ├── maps/                  # Folium HTML 지도 (LISA 클러스터 등)
│   └── final_summary.xlsx     # 8개 가설 종합표
└── final_report/
    ├── final_summary.xlsx
    ├── key_figures/           # fig1~fig6 발표 핵심 그림
    └── presentation_notes.md  # 발표 멘트 + 예상 Q&A
```

### 실행 순서
```powershell
uv run --python 3.11 python src/00_validate_data.py   # 데이터 검증
uv run --python 3.11 python src/h1_port.py            # H1 항만
uv run --python 3.11 python src/h2_road.py            # H2 도로
# ... h3 ~ h8 ...
uv run --python 3.11 python src/final_report.py       # 통합
streamlit run app.py                                  # 대시보드 실행
```

### 공통 분석 엔진 (`src/utils/stats.py`)
모든 가설이 공유하는 핵심 함수:
- **`run_ols(df, y_col, x_cols)`** — statsmodels OLS 실행 후 β·SE·p·유의성(별표)·R²·N·Pearson r을 한 딕셔너리로 반환
- **`calculate_vif()`** — 다중공선성 점검 (VIF ≥ 10 변수 제외 규칙)
- **`standardized_copy()`** — z-표준화 (그룹 간 β 비교용)
- **`conclusion_from_results()`** — 부호+유의성으로 `지지`/`부분지지`/`기각` 자동 판정

### 가설별 분석 로직 요약
- **H1~H4**: 단순 OLS가 기본. N=17 한계로 변수 2개 이하 제한. 산점도+회귀선 PNG, xlsx 표 동시 산출
- **H5**: `libpysal` Queen 인접 + KNN-2 가중행렬 → `esda`로 26산업 전역 Moran's I, 핵심 6산업 LISA(HH/LH/LL/HL 사분면 분류) → 클러스터 지도 HTML
- **H6**: 26산업 × 4요인 = β 매트릭스 생성 → 이론점수(수송비·전력·자본 5점 척도)와 실증 β 순위를 Spearman ρ로 비교 → **메인 히트맵**(RdBu_r) 생성
- **H7**: 연도별 wide 컬럼을 long으로 melt → `linearmodels.PanelOLS`로 시도+연도 고정효과 통제 → 단년 결과와 강건성 비교 (항만·IC는 시점불변이라 FE에 흡수되어 제외)
- **H8**: 광역시 8 / 도 9로 분할 → 각각 OLS → β 차이를 Z검정으로 유의성 판정
- **`final_report.py`**: h1~h8 산출물을 읽어 종합표·발표 핵심 그림(fig1~6)·발표 노트를 자동 생성

---

## 5. Streamlit 대시보드 (`app.py`)

발표용 인터랙티브 웹앱. 3가지 모드로 구성됩니다.

### 모드 1 — 탐색 (Explore)
- **시도별 산업 비중 단계구분도** (Folium choropleth, 연도·산업 선택)
- 선택 시도의 **입지요인 4종 카드** (항만·도로·전력·임금 — 17개 시도 대비 순위 + z-score)
- **어울리는 산업 TOP 3** (입지조건 기반 추천)
- 2020~2024 **시계열 변화** (해당 시도 vs 전국 평균)

### 모드 2 — 가설 결과 (Hypothesis)
- **H1~H8 결과 카드** — 각 가설을 일반인 언어로 풀어쓴 4단 해설
  (무엇을 확인했나 → 어떻게 검정했나 → 무엇이 나왔나 → 이게 무슨 뜻인가) + 지지/부분지지/제한 상태칩
- **인터랙티브 β 히트맵** — 26산업 × 4요인 민감도 (Plotly, 호버 시 β·p 표시)

### 모드 3 — 신규 산단 시뮬레이터 ⭐
프로젝트의 정책 데모 기능. 4단계로 작동:
1. **STEP 1** — 항만·IC·전력·임금 슬라이더로 가상 입지조건 설정 (실제 시도 프리셋 자동 입력 가능)
2. **STEP 2** — 입력값을 z-score 표준화 → 17개 시도 중 **가장 유사한 지역** 매칭
3. **STEP 3** — H6 회귀계수 × 입력 z-score로 **추천 산업 TOP 5** (적합도 0~100점) 산출
4. **STEP 4** — 입력 조건 진단 (z-score 막대 + 17개 시도 분포 박스플롯)

> 추천 로직: `적합도 = Σ(산업별 β × 입력요인 z-score)`를 0~100으로 정규화. 즉 "이 입지조건에 통계적으로 가장 잘 맞는 산업"을 제시.

### 디자인
- 미니멀·세련된 팔레트 (navy `#1d4ed8` 메인, 회색 보조), Pretendard/Apple SD Gothic 폰트
- Plotly 통일 테마, 카드형 UI, 반응형 레이아웃 (`@st.cache_data`로 데이터·지도 캐싱)

---

## 6. 기술 스택

| 영역 | 라이브러리 |
|---|---|
| 회귀분석 | `statsmodels` (OLS), `linearmodels` (PanelOLS FE) |
| 공간통계 | `libpysal` (Queen contiguity), `esda` (Moran's I, LISA), `geopandas`, `shapely` |
| 데이터 | `pandas`, `numpy`, `openpyxl`, `scipy`, `scikit-learn` |
| 시각화 | `matplotlib`, `seaborn` (정적), `plotly` (인터랙티브), `folium` (지도) |
| 대시보드 | `streamlit`, `streamlit-folium`, `branca` |
| 환경 | Python 3.11, `uv` 가상환경 |

---

## 7. 핵심 결과와 메시지

### 가설별 결론 (`presentation_notes.md` 기준)
- **H1** 조선·기타운송장비 무역항수 β=0.0004 → **부분지지**
- **H2** β=0.0013, R²=0.62 → **지지** (4개 요인 중 가장 명확)
- **H3** log전력 β=0.0005, R²=0.13 → **부분지지**
- **H4** 부호일치율 66.7% → **부분지지**
- **H5** 유의한 양의 Moran's I 산업 1/26개 → **부분지지**
- **H6** 3개 검정 중 1개 지지 → **부분지지**
- **H7** 강건 효과 0/16개 → **기각/제한적**
- **H8** 그룹 차이 유의 조합 5/24개 → **부분지지**

### 발표 핵심 메시지
> 한국 산업 입지는 **단일 요인으로 설명되지 않으며**, 물류·전력·임금 요인의 민감도가 **산업과 지역 유형(광역시/도)에 따라 다르게** 나타난다.

→ 정책 시사점: "모든 산업에 동일한 입지 정책"은 비효율적. 산업별 β 매트릭스로 **맞춤형 입지 전략**이 필요하다.

### 분석의 한계 (정직한 표현)
- **N=17의 한계**: 시도 관측치가 작아 단변량 회귀 중심. 패널·공간·그룹 분석은 보조 검증.
- **인과 ≠ 연관**: 정책·역사적 요인을 완전히 통제하지 못함 → 모든 결론을 "연관성" 수준으로 신중히 표현 (H7에서 단년 효과가 시도 FE에 대부분 흡수됨을 확인).
- 항만·IC는 5년 시점불변이라 패널 FE에서 검증 불가 (완전 공선성).

---

## 8. 코딩 규약 (CLAUDE.md 기준)

- 한국어 컬럼명 유지 (발표 자료 일관성)
- 시도 정렬: 서울→부산→…→제주 고정 순서, 약식명 + NFC 정규화
- p-value 별표: `*p<0.05, **p<0.01, ***p<0.001`
- 회귀 결과는 β·SE·p·R²·N 모두 보고
- 가설별 산출물 **3종 세트**: 콘솔 출력 + xlsx 저장 + PNG 저장
- 색상: navy(`#000080`) 메인 / 회색 보조, 히트맵 `RdBu_r`(0 대칭), 그림 dpi≥150
- 모든 함수 Google 스타일 docstring, 매직넘버는 모듈 상단 상수로 분리

---

*이 문서는 프로젝트 전체 코드(`app.py`, `src/` 8개 가설 스크립트, `utils/`, 산출물)를 정독해 작성한 설명서입니다.*
